# trading-backtester

## Run
From the repo root:

`powershell
python run_all.py
`",
",

`powershell
python self_test.py
`",
",

This repo ignores generated CSV/PNG outputs by default via .gitignore.
Use download_binance.py to create market_data.csv locally.
